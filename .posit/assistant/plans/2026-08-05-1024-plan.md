# Plan: Revise Directed Connectivity in lesson-graphs-connectivity.qmd

## Overview
The user wants me to revise `lesson-graphs-connectivity.qmd` so that it corresponds to the tripartite distinction (weak, unilateral, and strong connectivity) discussed in the presentation slides `slides/graph-connectivity.qmd`. 

Currently, `lesson-graphs-connectivity.qmd` only distinguishes between **Weakly Connected** and **Strongly Connected** graphs in the "Connectivity in Directed Graphs" section.

## Revisions Needed
1.  **Introduce Pairwise Connectivity States:** Before discussing the *whole graph*, I should introduce the three key pairwise states from the slides:
    -   *Pairwise Weakly Connected*: Connected via a semipath (ignoring arrow directions).
    -   *Pairwise Unilaterally Connected*: One-way reachability ($u \rightarrow v$ OR $v \rightarrow u$).
    -   *Pairwise Strongly Connected*: Mutual reachability ($u \rightarrow v$ AND $v \rightarrow u$).
    -   *(Optional but nice from slides)*: Pairwise Recursively Connected (mutual reachability using the exact same intermediate nodes).

2.  **Define Whole Graph Connectivity via the "Weakest Link" Rule:**
    -   A graph is **Strongly Connected** if *every* pair of nodes is pairwise strongly connected.
    -   A graph is **Unilaterally Connected** if *at least one* pair drops down to being only pairwise unilaterally connected (and no pair is worse). Meaning, for every pair, there is at least a one-way path.
    -   A graph is **Weakly Connected** if *at least one* pair drops down to being only pairwise weakly connected (connected only by a semipath).

3.  **Update the Component Section:**
    -   Ensure it still covers Strongly Connected Components (SCCs) and Weakly Connected Components (WCCs) accurately, but contextualized with the new tripartite terminology.

4.  **Add Visual Examples (using `ggraph`):**
    -   Create inline plots demonstrating strong, unilateral, and weak graph connectivity, mirroring the exact graph logic used in the slides (`gr_large_strong`, `gr_large_uni`, `gr_large_weak`).

## Next Steps
I will edit `lesson-graphs-connectivity.qmd` replacing the old `## Connectivity in Directed Graphs` section with this newly expanded tripartite classification, complete with the `ggraph` plotting code for the visual examples.